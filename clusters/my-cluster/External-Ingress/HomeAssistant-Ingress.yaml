apiVersion: v1
kind: Service
metadata:
  name: external-ha
  namespace: default
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8123
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-ha
  namespace: default
subsets:
  - addresses:
      - ip: 10.59.30.12  # Replace with actual IP
    ports:
      - port: 8123
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: external-ha-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  ingressClassName: traefik
  rules:
    - host: ha.swages.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: external-ha
                port:
                  number: 80
  tls:
    - hosts:
        - ha.swages.net
