apiVersion: v1
kind: Service
metadata:
  name: external-emby
  namespace: default
spec:
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8096
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-emby
  namespace: default
subsets:
  - addresses:
      - ip: 10.59.20.5  # Replace with actual IP
    ports:
      - port: 8096
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: external-emby-ingress
  namespace: default
  annotations:
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
    traefik.ingress.kubernetes.io/router.tls.certresolver: letsencrypt
spec:
  ingressClassName: traefik
  rules:
    - host: emby.swages.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: external-emby
                port:
                  number: 80
  tls:
    - hosts:
        - emby.swages.net
